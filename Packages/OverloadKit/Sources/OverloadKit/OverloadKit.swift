/// Shared foundation for apps built from the ios-studio factory.
public enum OverloadKit {
    /// Semantic version of the shared kit. Bump when you publish breaking API changes.
    public static let version = "0.2.0"
}
